<h1>Handlebars Funcionando</h1>

<h1>Mis productos</h1>

<div class="containerProducts">
    {{#each products}}
        <div class="card">

            <p> {{this.title}} </p>
            <p> {{this.description}} </p>
            <p> {{this.price}} </p>

        </div>
    {{/each}}
</div>